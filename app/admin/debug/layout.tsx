/** /admin/debug: sin guard del panel; diagnóstico directo en servidor. */
export const dynamic = "force-dynamic";
export const runtime = "nodejs";

export default function AdminDebugLayout({ children }: { children: React.ReactNode }) {
  return children;
}
